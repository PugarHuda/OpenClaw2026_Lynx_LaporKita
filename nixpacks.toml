# Railway / Nixpacks build config — backend (Python FastAPI) only.
# The web/ folder (Next.js) is deployed separately on Vercel.

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -e ."]

[start]
cmd = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
